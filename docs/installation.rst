============
Installation
============

At the command line::

    $ easy_install dbus_async

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dbus_async
    $ pip install dbus_async
